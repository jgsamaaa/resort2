import type { Metadata } from "next"; import PageHero from "@/components/PageHero"; import JournalCard from "@/components/JournalCard"; import CTASection from "@/components/CTASection"; import SectionHeading from "@/components/SectionHeading"; import { images, journalPosts } from "@/lib/amara-data";
export const metadata:Metadata={title:"Journal",description:"Travel notes, highland stories and slow itineraries from Amara Ridge."};export default function Journal(){return <><PageHero eyebrow="Field notes" title="Stories from a slower altitude." copy="Observations on the Philippine highlands, the people and produce around us, and the quiet art of travelling well." image={images.hero}/><section className="px-5 py-24 lg:px-10 lg:py-32"><div className="mx-auto max-w-[94rem]"><SectionHeading eyebrow="The Amara journal" title="Read before you rise."/><div className="mt-14 grid gap-x-8 gap-y-16 md:grid-cols-2">{journalPosts.map(post=><JournalCard key={post.slug} post={post}/>)}</div></div></section><CTASection/></>}
