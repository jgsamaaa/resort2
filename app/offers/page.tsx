import type { Metadata } from "next"; import Link from "next/link"; import { ArrowUpRight, Check } from "lucide-react"; import PageHero from "@/components/PageHero"; import CTASection from "@/components/CTASection"; import SectionHeading from "@/components/SectionHeading"; import { images, offers } from "@/lib/amara-data";
export const metadata:Metadata={title:"Stay Offers",description:"Curated romance, family, wellness and extended-stay journeys at Amara Ridge."};export default function Offers(){return <><PageHero eyebrow="Curated stays" title="A little more time, beautifully arranged." copy="Not discounts. Thoughtful ways to experience more of the ridge, with your villa, meals and rituals considered as one journey." image={images.firepit}/><section className="px-5 py-24 lg:px-10 lg:py-32"><div className="mx-auto max-w-[90rem]"><SectionHeading eyebrow="Amara journeys" title="Choose the feeling. We will shape the details."/><div className="mt-14 grid gap-6 md:grid-cols-2">{offers.map(offer=><article className="flex flex-col bg-soft-white p-7 sm:p-10" key={offer.slug}><p className="text-[9px] font-bold uppercase tracking-[.18em] text-clay">{offer.length} · {offer.bestFor}</p><h2 className="mt-5 font-display text-4xl sm:text-5xl">{offer.name}</h2><p className="mt-5 text-sm leading-7 text-pine-black/60">{offer.description}</p><ul className="mt-7 grid gap-3 border-t border-pine-black/10 pt-6 sm:grid-cols-2">{offer.includes.map(item=><li key={item} className="flex gap-2 text-xs text-pine-black/60"><Check className="h-4 w-4 text-moss"/>{item}</li>)}</ul><Link href={`/booking?offer=${offer.slug}`} className="mt-8 inline-flex items-center gap-2 self-start border-b border-pine-black pb-1 text-[10px] font-bold uppercase tracking-[.17em]">Request this journey <ArrowUpRight className="h-4 w-4"/></Link></article>)}</div><p className="mt-8 text-center text-xs leading-6 text-pine-black/50">Offers are subject to seasonal availability and are confirmed personally by the concierge. Inclusions may be adapted around weather and guest needs.</p></div></section><CTASection/></>}
