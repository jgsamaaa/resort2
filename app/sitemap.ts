import type { MetadataRoute } from "next"; import { journalPosts, stays } from "@/lib/amara-data";
export default function sitemap():MetadataRoute.Sitemap{const base="https://dalisay-cove-resort.vercel.app";const staticRoutes=["","/stays","/experiences","/wellness","/dining","/offers","/journal","/location","/gallery","/contact","/about","/booking"];return [...staticRoutes.map(route=>({url:`${base}${route}`,changeFrequency:"monthly" as const,priority:route===""?1:.8})),...stays.map(item=>({url:`${base}/stays/${item.slug}`,changeFrequency:"monthly" as const,priority:.7})),...journalPosts.map(item=>({url:`${base}/journal/${item.slug}`,changeFrequency:"yearly" as const,priority:.6}))];}
