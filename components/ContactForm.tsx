"use client";
import { FormEvent, useState } from "react";
import { CheckCircle2, Send } from "lucide-react";
export default function ContactForm() { const [sent,setSent]=useState(false); function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSent(true);e.currentTarget.reset();} const input="mt-2 w-full border border-pine-black/15 bg-white px-4 py-3 text-sm outline-none focus:border-clay"; const label="text-[9px] font-bold uppercase tracking-[.18em] text-clay"; if(sent)return <div className="bg-white p-10 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-moss"/><h2 className="mt-5 font-display text-4xl">Message received.</h2><p className="mt-4 text-sm text-pine-black/60">Our concierge will be in touch within one business day.</p><button onClick={()=>setSent(false)} className="mt-6 border-b border-pine-black text-[10px] font-bold uppercase tracking-[.16em]">Send another</button></div>; return <form onSubmit={submit} className="bg-soft-white p-6 sm:p-10"><div className="grid gap-5 sm:grid-cols-2"><label className={label}>Name<input required name="name" className={input}/></label><label className={label}>Email<input required type="email" name="email" className={input}/></label><label className={`${label} sm:col-span-2`}>Subject<select name="subject" className={input}><option>Stay planning</option><option>Private event</option><option>Dining inquiry</option><option>Press & partnerships</option></select></label><label className={`${label} sm:col-span-2`}>Message<textarea required name="message" rows={6} className={input}/></label></div><button className="mt-6 flex w-full items-center justify-center gap-3 bg-ember px-6 py-4 text-[10px] font-bold uppercase tracking-[.17em] hover:bg-pine-black hover:text-white">Send message <Send className="h-4 w-4"/></button></form>; }
